function adminMiddleware(req, res, next) {

    // if(req.body.category == "Administrador") {
    //     res.render('/');
    // } else {
    //     res.send("No eres usuario")
    // }

    // next();
}